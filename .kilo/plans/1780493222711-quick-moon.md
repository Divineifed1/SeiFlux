# SeiFlux Plan

## Current State
- Frontend is standalone Next.js app with mock data
- Backend was removed; server is compile-ready
- Navigation, footer, theme toggle are implemented
- Points-based rewards are implemented (easy 50, medium 75, hard 100)
- Project submission form includes Tech Stack field
- FluxWave pages: `/waves` and `/waves/[id]` are implemented
- Admin page includes Wave Status and Wave Points tracking

## Next Steps
1. Add GitHub OAuth on the frontend (NextAuth or similar)
2. Make `/admin` accessible only to admin GitHub user `Divineifed1`
3. Connect form submissions to GitHub login flow
4. Optional: re-add minimal backend for data persistence when needed
